# Indexer Service

Minimal worker service for indexing tasks.

## Running Locally

```bash
pip install -r requirements.txt
python main.py
```

## Docker

```bash
docker build -t indexer .
docker run indexer
```
